import React from 'react';

const ScanResults = () => (
  <div>
    <h2>Scan Results</h2>
    <p>View open ports, detected services, and mapped CVEs.</p>
    {/* TODO: Implement results table and export */}
  </div>
);

export default ScanResults; 