# ===================================================================
# VulneraMind Security Scanner - Python Dependencies
# Updated: August 21, 2025
# ===================================================================

# Core Web Framework & API Server
fastapi>=0.104.0                # Modern web framework for API server
uvicorn>=0.24.0                 # ASGI server for FastAPI
pydantic>=2.5.0                 # Data validation and settings management

# Network Scanning & Security Analysis
python-nmap>=0.7.0              # Network scanning and service detection
requests>=2.31.0                # HTTP client for API calls and banner grabbing

# AI & Language Models (Local)
ollama>=0.3.0                   # Local AI model integration for security analysis

# Database & Data Processing
# Note: sqlite3 is built into Python 3, no additional installation needed

# Data Processing & Parsing
msgpack>=1.0.5                  # Fast binary serialization (used in test.py)

# Optional Performance Enhancements
# pythonping>=1.1.4             # Better ping performance (commented out in host_discovery.py)

# Development & Testing Tools (Optional)
pytest>=7.0.0                   # Unit testing framework
black>=23.0.0                   # Code formatting
flake8>=6.0.0                   # Code linting

# ===================================================================
# System Dependencies (Install via package manager)
# ===================================================================

# Core Security Tools Required:
# - nmap                        # Network scanning engine
# - netcat-traditional (nc)     # Network listener and shell management  
# - exploitdb                   # Vulnerability and exploit database
# - metasploit-framework        # Penetration testing framework

# Development Tools (for exploit compilation):
# - gcc/build-essential         # C compiler for exploit compilation
# - perl                        # Perl interpreter for .pl exploits
# - ruby                        # Ruby interpreter for Metasploit modules
# - bash/sh                     # Shell interpreters for script execution

# ===================================================================
# Installation Commands by Operating System
# ===================================================================

# Ubuntu/Debian:
# sudo apt-get update
# sudo apt-get install nmap netcat-traditional exploitdb gcc build-essential perl ruby
# sudo apt-get install metasploit-framework  # or install manually
# sudo searchsploit -u

# Windows (WSL2 Recommended):
# 1. Install Windows Subsystem for Linux 2 (WSL2) with Ubuntu
# 2. Follow Ubuntu installation commands above
# 3. Install Ollama for local AI: curl -fsSL https://ollama.ai/install.sh | sh

# macOS (Homebrew):
# brew install nmap netcat exploitdb gcc perl ruby
# brew install --cask metasploit

# ===================================================================
# Python Environment Setup
# ===================================================================

# Tested on: Python 3.12+
# Minimum: Python 3.10+

# Virtual Environment Setup:
# python3 -m venv .venv
# source .venv/bin/activate      # Linux/macOS
# .venv\Scripts\activate         # Windows

# Install dependencies:
# pip install -r requirements.txt

# ===================================================================
# Additional Setup Required
# ===================================================================

# 1. Download NVD data (run once):
#    python backend/core/scanner/import_nvd_data.py --years 2023,2024,2025

# 2. Install and configure Ollama (for local AI):
#    curl -fsSL https://ollama.ai/install.sh | sh
#    ollama pull llama3.1          # or your preferred model

# 3. Verify system tools:
#    nmap --version
#    msfconsole --version
#    searchsploit --help